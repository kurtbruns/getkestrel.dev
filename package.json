{
  "name": "getkestrel.dev",
  "version": "1.0.0",
  "description": "Landing page and operator docs for Kestrel — getkestrel.dev",
  "type": "module",
  "private": true,
  "scripts": {
    "serve": "bash scripts/dev-hugo.sh",
    "dev:worker": "npm run docs:sync && hugo --minify && wrangler dev",
    "docs:sync": "node scripts/sync-docs.mjs",
    "shots": "node scripts/shots.mjs",
    "hugo:check": "scripts/check-hugo.sh"
  },
  "keywords": [
    "kestrel",
    "newsletter",
    "self-hosted",
    "hugo"
  ],
  "author": "Kurt Bruns",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^4.131.1"
  }
}
